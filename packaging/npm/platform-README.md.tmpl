# {{.Name}}

This package holds the OwnGit {{.Version}} executable for {{.Platform}}. Install the `owngit` package instead, which selects this package on matching systems:

```sh
npm install -g owngit
```

See the [project README]({{.Homepage}}) for other installation routes.
